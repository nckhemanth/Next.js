# AGENTS.md

This is a public Next.js learning repo.

If the private `workspace` hub is available, read this first:

- `workspace/context/repos/Next.js/CONTEXT.md`
- `workspace/context/repos/Next.js/CHANGELOG.md`

Rules:

- Do not commit secrets, `.env` values, raw transcripts, or private notes.
- Keep explanations current with official Next.js, React, Auth.js, Sanity, Sentry, and AI SDK docs.
- If a video/course pattern is version-specific, label it with the version instead of presenting it as universal.
- Prefer practical examples, route trees, lifecycle diagrams, and interview answers over vague summaries.
- Keep labs runnable with `npm run typecheck` from the repo root.

